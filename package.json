{
  "name": "linter-yaml-lint",
  "main": "./lib/init",
  "version": "0.1.0",
  "description": "Lint YAML on the fly, using yaml-lint",
  "repository": "https://github.com/alexweber/linter-yaml-lint",
  "license": "MIT",
  "scripts": {
    "lint": "./node_modules/.bin/coffeelint ./lib"
  },
  "engines": {
    "atom": ">=1.0.0"
  },
  "dependencies": {
    "atom-linter": "^3.2.0",
    "atom-package-deps": "^2.0.2"
  },
  "package-deps": [
    "linter"
  ],
  "providedServices": {
    "linter": {
      "versions": {
        "1.1.0": "provideLinter"
      }
    }
  },
  "devDependencies": {
    "coffeelint": "^1.12.1"
  }
}
